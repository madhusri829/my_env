name: AI-Browser-Organizer
version: 1.0.0
author: "Madhusri Gone"
description: "AI-driven browser history organizer with safety detection and usage tracking."

# This points to the class inside your env.py file
entry_point: "env:BrowserOrganizerEnv"

# The main logic and task files
env_file: "env.py"
task_file: "tasks.json"

# List of libraries required to run your environment
dependencies:
  - gymnasium
  - numpy
  - datetime